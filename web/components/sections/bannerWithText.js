import Sanity from "../sanity-image"

export default function BannerWithText(props) {
  
  const {image,title, description, lear_more} = props;

  return (

    <section className="px-4 md:px-0 md:max-w-[93.3%] w-full mx-auto flex flex-col">

    </section>

  )
}
